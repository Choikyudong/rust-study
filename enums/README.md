# 열거형
1. 열거형은 다음과 같이 선언한다.
   enum <enum이름> {
      Variant
   }
2. 열거형에는 배리언트들을 선언할 수 있다.
3. [얼거형::배리언트] 로 사용할 수 있다.
4. 열거형도 [메서드]를 만들 수 있다.
5. 러스트에는 "null" 개념이 없다.
6. null 개념을 표현하기 위해 표준 라이브러리에서 Option enum 을 사용할 수 있다.
   많이 사용하는 라이브러리이기 때문에 기본적으로 임포트 되어 있다.
