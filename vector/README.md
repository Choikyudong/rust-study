# Vector
컬렉션 타입 중 하나로 Vec<T>이다.
1. 제네릭을 이용하며 <> 안에 원하는 타입을 지정하면 된다.
2. vec![원소1, 원소2] 로 생성도 가능하다.
3. 값을 접근하는 방법은 대표적으로 get과 &를 이용하는 것이다.
4. 러스트의 벡터는 공간이 부족할 경우 새로운 공간에 모든 벡터의 값을 그대로 가져간 뒤 그 뒤에 새로운 값을 붙인다.