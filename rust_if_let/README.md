# it let
1. if let을 사용하여 간결하게 표현을 할 수 있다.
   match를 사용할 때에 enum의 베리언트가 있는 만큼
   정의를 해줘야하는데 이게 의미가 없는 코드를 작성할 수 있다.
   이럴 때 it let으로 간결하게 작성을 할 수 있다.
2. 사용방법
   if let <타입> = 변수 { /* 코드 */ }
   이렇게 사용하면 보일러 플레이트 코드를 덜 쓰게 된다.