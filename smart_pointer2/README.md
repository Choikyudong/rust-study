# 스마트 포인터
1. `RC<T>`는 소유자를 여러명 이상일 때 사용된다.  
   `RC`의 참조자를 카운팅(**strong_count**)하여 0일 경우 메모리를 정리한다.
   기본적으로 읽기 전용이다.
2. `RefCell<T>`는 내부 가변성으로 불변 참조자가 있더라도 데이터를 변경할 수 있게 해준다.  
   `RefCell`의 불변성과 대여 규칙은 런타임에 진행된다.